# Use the SAME password you use to connect in MySQL Workbench (root password).
# Replace the value below with your actual MySQL password, then save this file.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=hackathon_db
